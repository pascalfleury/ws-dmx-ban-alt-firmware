* General functionality

The board is equipped with dimmers for LEDs, and has leds in several white temperatures. The goal is to be able to drive the dimming and the color temperature separately through DMX, while the firmware will do the correct "mixing" of warm and cold light to get to the wanted temperature.

* Hardware Board

The board is based on the processor STC8H1K16 (http://www.stcmicro.com/datasheet/STC8H-en.pdf)
It exists in several version, with 3, 4, 5 or 6 PWM outputs.
Currently we use the 3 output version.

** General setup
Board clock: 24MHz
UART: 250000 baud
Memory:

** Pinout
As far as I can tell, this is the pinout:

| pin | port | function              |
|-----+------+-----------------------|
|   1 | P1.0 | pwm O3                |
|   2 | P1.1 |                       |
|   3 | P1.2 | pwm O2                |
|   4 | P1.6 |                       |
|   5 | P1.7 | pwm O1                |
|   6 |      | AGnd                  |
|   7 |      | ADC_Vref+             |
|   8 |      | AVcc                  |
|-----+------+-----------------------|
|   9 | P5.4 |                       |
|  10 | Vcc  |                       |
|  11 | P5.5 |                       |
|  12 | Gnd  |                       |
|  13 | P3.0 | (RxD) prog connector? |
|  14 | P3.1 | (TxD) prog connector? |
|  15 | P3.2 | DMX512 LED            |
|  16 | P3.3 | DIP 1                 |
|-----+------+-----------------------|
|  17 | P3.4 | DIP 2                 |
|  18 | P3.5 | DIP 3                 |
|  19 | P3.6 | DIP 4                 |
|  20 | P3.7 | DIP 5                 |
|  21 | P2.0 |                       |
|  22 | P2.1 |                       |
|  23 | P2.2 |                       |
|  24 | P2.3 |                       |
|-----+------+-----------------------|
|  25 | P2.4 | DIP 6                 |
|  26 | P2.5 | DIP 7                 |
|  27 | P2.6 | DIP 8                 |
|  28 | P2.7 | DIP 9                 |
|  29 | P0.0 | DIP 10                |
|  30 | P0.1 |                       |
|  31 | P0.2 |                       |
|  32 | P0.3 |                       |

* DMX
The software will expose several modes to help with driving the R/G/B channels. Modes are a collection of channels.

** Channels

- #1: general dimmer (coarse)
- #2: general dimmer (fine)
- #3: color temperature (coarse)
- #4: color temperature (fine)
- #5: strobe mode (no strobe, flashing, lightning)
- #6: strobe speed (slow -> fast)

** Modes

*** Simple mode (4 channels)
This exposes these channels:
1. general dimmer (coarse)
2. color temperature (coarse)
3. strobe mode
4. strobe speed

*** Full mode (6 channels)
This exposes these channels:
1. general dimmer (coarse)
2. general dimmer (fine)
3. color temperature (coarse)
4. color temperature (fine)
5. strobe mode
6. strobe speed

* Development Environment
** Dev tools
I am using stcgal and sdcc as the open-source development tools.

** Global Coding Rules
*** Functionality
- Never use floating point (strict constraint).
- All DMX buffer logic must reside in ~dmx.c~.

*** Style
Always use the '1TBS' (One True Brace Style) for C code.
  - Braces on the same line as 'if', 'while', 'for'.
  - Use 2 spaces for indentation (no tabs).
  - Keep the 'else' on the same line as the closing brace: '} else {'.
Do not try to fix style yourself at the end, I will do it.
